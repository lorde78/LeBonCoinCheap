{% extends 'base.html.twig' %}

{% block title %}{{ title }}{% endblock %}

{% block content %}

    {% for article in articles %}
        <div >
            <h3>

        {{ article.title }}
            </h3>
            <div>
                {{ article.description }}
            </div>

        </div>

    {% endfor %}


{% endblock %}
